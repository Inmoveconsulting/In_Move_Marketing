<%- include('../partials/header') %>

<h1>Historial de versiones — identidad visual</h1>
<p><a href="/productos/<%= producto.slug %>/identidad-visual">&larr; volver a la identidad visual actual</a></p>

<table class="table">
  <thead>
    <tr>
      <th>Versión</th>
      <th>Estado</th>
      <th>Estilo</th>
      <th>Intentos</th>
      <th>Creado</th>
      <th>Aprobado</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% versiones.forEach(function (v) { %>
      <tr>
        <td>v<%= v.version %></td>
        <td><span class="badge badge-<%= v.estado %>"><%= v.estado === 'aprobado' ? 'Aprobado' : 'Borrador' %></span></td>
        <td><%= v.estilo || '—' %></td>
        <td><%= v.intentos %></td>
        <td><%= new Date(v.creado_en).toLocaleDateString('es-AR') %></td>
        <td><%= v.aprobado_en ? new Date(v.aprobado_en).toLocaleDateString('es-AR') : '—' %></td>
        <td><a href="/productos/<%= producto.slug %>/identidad-visual/version/<%= v.version %>">Ver</a></td>
      </tr>
    <% }); %>
    <% if (!versiones.length) { %>
      <tr><td colspan="7" class="muted">Todavía no hay versiones.</td></tr>
    <% } %>
  </tbody>
</table>

<%- include('../partials/footer') %>
