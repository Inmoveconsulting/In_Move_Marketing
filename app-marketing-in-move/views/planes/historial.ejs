<%- include('../partials/header') %>

<h1>Historial de versiones — plan de marketing</h1>
<p><a href="/productos/<%= producto.slug %>/plan">&larr; volver al plan actual</a></p>

<table class="table">
  <thead>
    <tr>
      <th>Versión</th>
      <th>Estado</th>
      <th>Objetivo</th>
      <th>Duración</th>
      <th>Creado</th>
      <th>Aprobado</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% versiones.forEach(function (v) { %>
      <tr>
        <td>v<%= v.version %></td>
        <td><span class="badge badge-<%= v.estado %>"><%= v.estado === 'aprobado' ? 'Aprobado' : 'Borrador' %></span></td>
        <td class="truncate"><%= v.objetivo %></td>
        <td><%= v.duracion_dias ? v.duracion_dias + ' días' : '—' %></td>
        <td><%= new Date(v.creado_en).toLocaleDateString('es-AR') %></td>
        <td><%= v.aprobado_en ? new Date(v.aprobado_en).toLocaleDateString('es-AR') : '—' %></td>
        <td><a href="/productos/<%= producto.slug %>/plan/version/<%= v.version %>">Ver</a></td>
      </tr>
    <% }); %>
    <% if (!versiones.length) { %>
      <tr><td colspan="7" class="muted">Todavía no hay versiones.</td></tr>
    <% } %>
  </tbody>
</table>

<%- include('../partials/footer') %>
